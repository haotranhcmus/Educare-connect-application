legacy-peer-deps=true
# NEVER run: npm audit fix --force
# It will downgrade expo 54 → 49 breaking the whole Metro/worklets chain.
# The 4 moderate vulns are in postcss inside expo's build tooling — not exploitable at runtime.
audit=false
