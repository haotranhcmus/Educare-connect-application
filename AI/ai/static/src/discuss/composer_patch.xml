<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="mail.Composer" t-inherit="mail.Composer" t-inherit-mode="extension">
        <!-- Hide emoji button in AI chat (no JS property to control this) -->
        <xpath expr="//button[@t-ref='emoji-button']" position="attributes">
            <attribute name="t-if">!props.composer.thread or props.composer.thread.channel_type !== 'ai_chat'</attribute>
        </xpath>
    </t>
    
    <!-- Override send button template to always show in AI chat -->
    <t t-name="mail.Composer.sendButton" t-inherit="mail.Composer.sendButton" t-inherit-mode="extension">
        <xpath expr="//button[hasclass('o-mail-Composer-send')]" position="attributes">
            <attribute name="t-if">(!compact or ui.isSmall or props.composer.thread?.channel_type === 'ai_chat')</attribute>
        </xpath>
    </t>
</templates>
